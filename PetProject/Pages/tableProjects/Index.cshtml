@page
@model PetProject.Pages.tableProjects.IndexModel

@{
    ViewData["Title"] = "Table Projects";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h1>Table Projects</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.tableProjects[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.tableProjects[0].Disctiption)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.tableProjects) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Disctiption)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
